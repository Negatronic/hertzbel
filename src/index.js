import {
  toHz,
  toKiloHz,
  toMegaHz,
  toGigaHz,
  toTeraHz,
  addFreq,
  subFreq,
  multFreq,
  divFreq,
} from "./hertz";

import { toDbm, toDbw, toDbuw } from "./decibel";
console.log(toDbm(2));
